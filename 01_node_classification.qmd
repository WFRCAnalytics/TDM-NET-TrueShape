---
title: Node Classification
subtitle: classifying network nodes by type and connectivity
description: A short description
author:
 - name: Pukar Bhandari
   email: pukar.bhandari@wfrc.utah.gov
   affiliation:
     - name: Wasatch Front Regional Council
       url: "https://wfrc.utah.gov/"
date: "2026-03-13"
---

## Setup Environment

### Import Libraries

```{python}
import sys
from pathlib import Path

import pandas as pd
import geopandas as gpd

sys.path.insert(0, str(Path.cwd()))
from _src.node_utils import classify_nodes
```

### Filepath Configuration

```{python}
config_filepath = {
    # Input files
    "in_centerlines": Path("_data/raw/UtahRoads.gpkg"),
    "in_network_links": Path("_data/raw/WFv1000_MasterNet - Link.gpkg"),
    "in_network_nodes": Path("_data/raw/WFv1000_MasterNet - Node.gpkg"),
    # Output files
    "out_node_classified": Path("_data/processed/nodes_classified.gpkg"),
}
```

## Load Data

### Roadway Centerlines

```{python}
gdf_centerlines = gpd.read_file(config_filepath["in_centerlines"])

print(f"Loaded {len(gdf_centerlines):,} features | CRS: {gdf_centerlines.crs}")
```

```{python}
# | eval: false

gdf_centerlines.head(1000).explore()
```

### Network Links

```{python}
gdf_links = gpd.read_file(config_filepath["in_network_links"])

print(f"Loaded {len(gdf_links):,} features | CRS: {gdf_links.crs}")
```

```{python}
#| eval: false

gdf_links.head(1000).explore()
```

### Network Nodes

```{python}
gdf_nodes = gpd.read_file(config_filepath["in_network_nodes"])

print(f"Loaded {len(gdf_nodes):,} features | CRS: {gdf_nodes.crs}")
```

```{python}
#| eval: false

gdf_nodes.head(1000).explore()
```

## Node Classification

### Classify Nodes

```{python}
gdf_nodes_classified = classify_nodes(gdf_nodes, gdf_links, ft_year=2023)

# Preview classification columns only
classification_cols = [
    "N",
    "Freeway",
    "Expressway",
    "Arterial",
    "Collector",
    "Local",
    "Ramp",
    "CentroidConnector",
    "FixedTransit_byFT",
    "FixedTransit_byN",
    "LinkCount",
]
gdf_nodes_classified[classification_cols].head(20)
```

### Review FixedTransit Methods

Compare the two independent FixedTransit classification methods to decide which to keep.

```{python}
ft_comparison = gdf_nodes_classified[["N", "FixedTransit_byFT", "FixedTransit_byN"]].copy()
ft_comparison["agree"] = ft_comparison["FixedTransit_byFT"] == ft_comparison["FixedTransit_byN"]

# Inspect disagreeing rows if any exist
ft_comparison[~ft_comparison["agree"]]
```

### Summary

```{python}
flag_cols = [
    "Freeway", "Expressway", "Arterial", "Collector", "Local",
    "Ramp", "CentroidConnector", "FixedTransit_byFT", "FixedTransit_byN",
]
gdf_nodes_classified[flag_cols].sum().rename("NodeCount").to_frame()
```

```{python}
out_path = config_filepath["out_node_classified"]

out_path.parent.mkdir(parents=True, exist_ok=True)
gdf_nodes_classified.to_file(out_path, driver="GPKG")
print(f"Classified Nodes saved to: {out_path}")
```
